node_modules/
dist/
release/
coverage/
*.log


# Certificado SII, contrasenas y respaldos de DTE (datos reales, nunca al repo)
sii_data/
*.pfx

# Ignored release artifacts (installers and blockmaps)
release/*.exe
release/*.blockmap
